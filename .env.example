# Polymarket Auto-Claim Bot Configuration

# Check interval (cron format)
# Default: */10 * * * * (every 10 minutes)
# Examples:
#   */5 * * * *  = Every 5 minutes
#   */15 * * * * = Every 15 minutes
#   */30 * * * * = Every 30 minutes
#   0 * * * *    = Every hour
CHECK_INTERVAL=*/10 * * * *

# Browser headless mode
# Set to 'false' to see the browser window (useful for debugging)
# Set to 'true' for background operation
HEADLESS=true
